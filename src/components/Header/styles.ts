import styled from 'styled-components';

export const Head = styled.header`
    background: #0062be;

    div {
        max-width: 1120px;
        margin: 0 auto;
        padding: 2rem 1rem 12rem;
    }
`;